const Theme = {
  LIGHT: 'light-theme',
  DARK: 'dark-theme',
};

const changeRef = document.querySelector('#theme-switch-toggle');
const bodyRef = document.querySelector('body');
changeRef.addEventListener("click", changeTheme);

function changeTheme() {
  
}